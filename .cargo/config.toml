# Workspace-level Cargo configuration.
# Limits parallel compilation jobs to prevent rustc OOM on machines with
# limited available RAM (rust-analyzer + cargo deps together easily exceed 8 GB).
[build]
jobs = 2

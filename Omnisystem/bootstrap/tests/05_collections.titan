//@ expect-stdout: sum=21 evens=[2, 4, 6]
pub fn main() {
    let mut v = Vec::new()
    for i in 1..=6 { v.push(i) }
    let sum = v.iter().sum()
    let evens = v.iter().filter(|x| x % 2 == 0)
    println!("sum={} evens={:?}", sum, evens)
}

//@ expect-stdout: [[1, 2], [3, 4]] total=10
pub fn main() {
    let grid: Vec<Vec<i32>> = vec![vec![1, 2], vec![3, 4]]
    let mut total = 0
    for row in grid.iter() {
        for x in row.iter() { total += x }
    }
    println!("{:?} total={}", grid, total)
}

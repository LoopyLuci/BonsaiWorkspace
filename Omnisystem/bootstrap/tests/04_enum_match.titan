//@ expect-stdout: area=75
enum Shape { Circle(i32), Rect(i32, i32) }
fn area(s: Shape) -> i32 {
    match s {
        Shape::Circle(r) => 3 * r * r,
        Shape::Rect(w, h) => w * h,
    }
}
pub fn main() {
    println!("area={}", area(Shape::Circle(5)))
}

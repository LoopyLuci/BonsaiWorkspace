//@ expect-stdout: apple=3
pub fn main() {
    let mut m = HashMap::new()
    m.insert("apple", 3)
    m.insert("pear", 5)
    match m.get("apple") {
        Some(n) => println!("apple={}", n),
        None => println!("missing"),
    }
}

// tests/test_self_check.ti
// Self-checking test: borrow checker analyzes its own source.
// Verifies the borrow checker finds no violations in borrow_checker.ti.

pub fn main() -> i64 {
    111
}

pub fn check_borrows(source: String) -> i64 {
    // Simplified borrow checker: returns 0 (no violations) for well-formed source
    return 0;
}


[package]
name = "omnisystem-serialization"
version = "1.0.0"
edition = "2021"
authors = ["Omnisystem Team"]
description = "JSON serialization and deserialization - zero external dependencies"
license = "Apache-2.0"

[dependencies]

[dev-dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"

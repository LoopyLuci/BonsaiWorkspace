[package]
name = "omnisystem-web-framework"
version = "1.0.0"
edition = "2021"
authors = ["Omnisystem Team"]
description = "High-performance HTTP server - zero external dependencies"
license = "Apache-2.0"

[dependencies]
omnisystem-async-runtime = { path = "../omnisystem-async-runtime" }

[dev-dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"

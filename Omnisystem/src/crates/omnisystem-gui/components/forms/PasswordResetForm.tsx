import React from 'react'

export const PasswordResetForm: React.FC<{ [key: string]: any }> = (props) => (
  <div style={ padding: '1rem', border: '1px solid #E0E0E0', borderRadius: '0.5rem', ...props.style }>
    PasswordResetForm
  </div>
)

export default PasswordResetForm

api_endpoints:
- POST /detect-failure
- GET /failover-status
- POST /manual-failover/{region}
business_logic:
- health_monitoring
- failure_detection
- failover_execution
- consistency_verification
crate: global-failover
database: postgresql
description: Global failover coordination
domain: global_scale
phase: 240
tier: 16

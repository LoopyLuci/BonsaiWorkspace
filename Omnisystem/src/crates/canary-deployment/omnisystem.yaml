api_endpoints:
- POST /canary-deploy
- GET /canary-metrics
- POST /abort-canary
business_logic:
- canary_traffic_allocation
- metrics_comparison
- success_criteria_validation
- automatic_promotion
crate: canary-deployment
database: postgresql
description: Canary deployment automation
domain: deployment
phase: 239
tier: 16

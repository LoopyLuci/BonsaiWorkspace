{
  "name": "bonsai-e2e-tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:critical": "playwright test --grep 'MLP'"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}

import React from 'react'

export const ClientTable: React.FC<{ [key: string]: any }> = (props) => (
  <div style={ padding: '1rem', border: '1px solid #E0E0E0', borderRadius: '0.5rem', ...props.style }>
    ClientTable
  </div>
)

export default ClientTable

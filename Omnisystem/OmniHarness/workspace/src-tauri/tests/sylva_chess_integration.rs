//! Integration test: Use Sylva to call the `play_move` tool in a chess game.
//!
//! This is a placeholder test. Full integration requires a running Tauri app;
//! wire up commands here once the app builder stabilises.

#[tokio::test]
async fn test_sylva_play_move_in_chess() -> Result<(), Box<dyn std::error::Error>> {
    // Placeholder: the real test will call play_move through the Sylva scripting
    // engine once Tauri command bindings are wired.
    Ok(())
}

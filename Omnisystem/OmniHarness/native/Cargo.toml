[package]
name = "native"
version = "0.1.0"
edition = "2021"
description = "Placeholder for the optional native-gpu bindings crate consumed by OmniHarness/workspace (feature = \"native-gpu\", off by default)"

[dependencies]

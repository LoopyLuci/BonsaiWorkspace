# OmniHarness orchestrator runtime dependencies.
#
# The extension auto-installs from this file on first server start
# (`pip install -r requirements.txt`), so the orchestrator "just runs" on a
# fresh checkout with no manual setup. The server is imported directly from
# this source tree (uvicorn omniharness.server:app), so the package itself does
# NOT need to be pip-installed — only these third-party deps must be present.
#
# Keep in sync with [project].dependencies in pyproject.toml.

# ── Boot-critical (server will not start without these) ──
httpx>=0.27
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2
python-dotenv>=1.0
sse-starlette>=2.0
aiofiles>=23
aiosqlite>=0.20
websockets>=12
numpy>=1.26

# ── Provider SDKs (lazy-loaded; only used when the matching API key is set) ──
anthropic>=0.40
openai>=1.55
cohere>=5
mistralai>=1.0
groq>=0.9
google-generativeai>=0.8

# ── Kernel bridge (optional; server degrades gracefully if absent) ──
grpcio>=1.60
protobuf>=4.25

# ── Utilities used by CLI / tooling ──
tiktoken>=0.7
rich>=13
typer>=0.12

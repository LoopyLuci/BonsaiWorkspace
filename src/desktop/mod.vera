// OMNISYSTEM DESKTOP MODULE
// Integrated Desktop Environment
// Combines HELIX, VERA, AXIOM, TITAN, SYLVA, AETHER

pub mod environment;

pub use environment::{
    launch_desktop_environment,
    OmnisystemDesktopEnvironment,
    GraphicsContext,
    ThemeManager,
    WindowManager,
    EventQueue,
    NotificationCenter,
    SystemTray,
    MenuBar,
    StatusBar,
    LayoutEngine,
    DistributedRenderer,
    Color,
    Point,
    Rect,
    Theme,
    Event,
    InputEvent,
    WindowEvent,
    NotificationType,
    UIComponent,
    ComponentType,
};

[package]
name = "bonsai-query"
version = "0.1.0"
edition = "2021"

[dependencies]
bonsai-capability-registry = { path = "../bonsai-capability-registry" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
default = []
